import { Settings } from 'lucide-react';
import { PageScaffold } from '../../components/common/PageScaffold';

export const AdminSettingsPage = () => (
  <PageScaffold title="Cài đặt" subtitle="Cấu hình hệ thống" icon={Settings} />
);
